---
title: PetraDB
description: A PostgreSQL-syntax-compatible SQL database that runs in-memory, in the browser, or on disk. No Docker, no server — just import and query.
template: splash
hero:
  tagline: A PostgreSQL-syntax-compatible SQL database that runs in-memory, in the browser, or on disk. No Docker, no server — just import and query.
  actions:
    - text: Get Started in JavaScript / TypeScript
      link: /getting-started/javascript/
      icon: right-arrow
    - text: Get Started in Scala
      link: /getting-started/scala/
      icon: right-arrow
    - text: Try the Playground
      link: /playground/
      variant: minimal
      icon: right-arrow
    - text: GitHub
      link: https://github.com/edadma/petradb
      variant: minimal
      icon: external
    - text: llms.txt
      link: /llms.txt
      variant: minimal
      icon: document
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<CardGrid stagger>
	<Card title="npm install and go" icon="rocket">
		Add a real SQL database to any JavaScript or TypeScript project as a dependency — no Docker, no native bindings, no server process.
	</Card>
	<Card title="Scala cross-project" icon="setting">
		Written in Scala 3, cross-compiled to JVM, Scala.js, and Scala Native. Use the same database library across platforms from a single codebase.
	</Card>
	<Card title="PostgreSQL-syntax-compatible" icon="pencil">
		Real SQL — DDL, DML, joins, aggregations, subqueries, transactions, and prepared statements with familiar PostgreSQL syntax.
	</Card>
	<Card title="In-memory by default" icon="star">
		Spin up a full SQL database in one line. Perfect for tests, prototyping, browser apps, and anywhere you need SQL without infrastructure.
	</Card>
	<Card title="Grows with you" icon="document">
		Start in-memory for fast iteration, then switch to crash-safe persistent storage in a single file — no migration, no config change.
	</Card>
	<Card title="Runs in the browser" icon="laptop">
		The engine compiles to JavaScript with zero native dependencies, so it works in Node.js, Deno, Bun, and directly in the browser.
	</Card>
</CardGrid>